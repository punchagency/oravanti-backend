# Service role key used by the one-shot bucket bootstrap container.
# Must match SERVICE_KEY in .env.storage and SUPABASE_SERVICE_ROLE_KEY in the API .env.
SERVICE_ROLE_KEY=replace-with-local-service-role-jwt
