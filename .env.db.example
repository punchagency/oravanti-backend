# Local Postgres settings.
# POSTGRES_PASSWORD must match the passwords hardcoded in docker/supabase/init/00-roles.sql
# unless you update that init SQL too.
POSTGRES_DB=postgres
POSTGRES_PASSWORD=postgres
