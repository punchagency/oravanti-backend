# One-shot database bootstrap settings.
# This service runs docker/supabase/init/00-roles.sql against existing DB volumes.
# It fixes cases where Postgres init SQL did not run because the volume already existed.
DATABASE_URL=postgres://postgres:postgres@db:5432/postgres
